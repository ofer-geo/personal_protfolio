---
import ProjectLayout from '../../layouts/ProjectLayout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<ProjectLayout
  title="Near-Home Parking: Shortest-Path Lot Assignment Model"
  year="2025"
  category="Network Analysis · Urban Parking"
  tools={['Python', 'OSMnx', 'NetworkX', 'GeoPandas', 'QGIS', 'Shortest Path']}
>

  <section class="proj-section">
    <h2 class="proj-section-title">Background</h2>
    <div class="proj-body">
      <p>
        Tel Aviv's "Near-Home Parking" policy gives residents priority overnight access to municipal
        lots near their homes. As the policy expanded, the city faced a concrete planning question —
        which streets should be assigned to which lot? The existing boundaries were drawn by hand and
        ignored real walking distances, so this project built a spatial model to assign every street to
        its nearest available lot while balancing demand across the network.
      </p>
    </div>
  </section>

  <section class="proj-section">
    <h2 class="proj-section-title">Maps</h2>
    <img
      src={`${base}/maps/night-parking-load-index.jpg`}
      alt="City-wide map showing the load index of near-home parking lots across Tel Aviv quarters"
      class="proj-map"
      loading="lazy"
    />
    <p class="proj-map-caption">
      Parking lot load index across Tel Aviv — circle size proportional to assigned resident demand.
      Identifies overloaded lots and guides capacity planning.
    </p>
    <img
      src={`${base}/maps/night-parking-service-area.jpg`}
      alt="Map comparing current vs model-derived service area for HaHashmal near-home parking lot"
      class="proj-map"
      loading="lazy"
    />
    <p class="proj-map-caption">
      Single-lot example (HaHashmal): administratively drawn service boundary (red) vs. model-derived
      catchment based on shortest-path walking distance (green).
    </p>
  </section>

  <section class="proj-section">
    <h2 class="proj-section-title">Methodology</h2>
    <ul class="proj-list">
      <li><strong>Street network:</strong> Extracted the pedestrian network from <strong>OpenStreetMap</strong> with <strong>OSMnx</strong>, weighting edges by real walking distance.</li>
      <li><strong>Shortest-path assignment:</strong> Computed walking distances from every residential address to all lots using <strong>NetworkX</strong>, assigning each street to its closest option.</li>
      <li><strong>Capacity balancing:</strong> Iteratively re-routed overflow demand to the next-nearest lot once capacity was reached — run across 12 synthesis rounds as new lots joined the policy.</li>
      <li><strong>Load index:</strong> Scored each lot by assigned demand relative to capacity, flagging those under pressure.</li>
      <li><strong>Service areas:</strong> Derived lot catchment polygons directly from the assignment output, replacing the hand-drawn administrative boundaries.</li>
    </ul>
  </section>

  <section class="proj-section">
    <h2 class="proj-section-title">Results</h2>
    <div class="proj-body">
      <p>
        The model's lot assignments and service-area boundaries were <strong>adopted into the
        municipality's production system</strong>, replacing the hand-drawn administrative map and
        becoming the operational basis for the policy citywide.
      </p>
    </div>
  </section>

</ProjectLayout>

<style>
  :global(.proj-list) {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
    max-width: 68ch;
  }
  :global(.proj-list li) {
    font-size: 0.9rem;
    line-height: 1.7;
    color: #2A2A2A;
    padding-left: 1.2em;
    position: relative;
  }
  :global(.proj-list li::before) {
    content: '—';
    position: absolute;
    left: 0;
    color: #3D5E35;
    font-weight: 600;
  }
</style>
